/*
package id.rockierocker.elasticsearchkafkaconsumer.service;

import id.rockierocker.elasticsearchkafkaconsumer.entity.Book;
import org.springframework.kafka.annotation.KafkaListener;
import org.springframework.stereotype.Service;

@Service
public class KafkaConsumerServiceImpl {

    @KafkaListener(topics = "elastic-search", groupId = "11")
    public void consume(Book object) {
        System.out.println("Received message: " +object);
    }

}
*/
